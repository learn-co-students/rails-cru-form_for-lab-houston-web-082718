<h1>Artists:</h1>
<p><%= link_to 'Add Artist', new_artist_path %> | <%= link_to 'View Genres', genres_path %> | <%= link_to 'View Songs', songs_path %></p>

<% @artists.each do |artist|%>
    <h2><%= link_to artist.name, artist_path(artist) %></h2>
    <p><%= artist.bio %></p>
<% end %>