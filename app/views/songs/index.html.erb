<h1>Songs:</h1>
<%= link_to 'New Song', new_song_path %>
<% @songs.each do |song|%>
    <p><%= link_to song.name, song_path(song) %></p>
    <p><%= link_to song.artist.name, artist_path(song.artist) %> | <%= link_to song.genre.name, genre_path(song.genre) %></p>
<% end %>